---
interface Props {
  title: string;
  subtitle?: string;
  class?: string;
}
---

<article
  class={`
  ${Astro.props.class}
  relative rounded-xl
  backdrop-blur-md
  border border-black/10
  shadow-white/10 shadow-inner
  group
`}
>
  <!-- Añadir un fade in negro para que se vea el contenido -->
  <slot name="image" />
</article>
